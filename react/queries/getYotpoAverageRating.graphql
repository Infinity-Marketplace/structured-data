query GetProductRating($productId: String) {
  getProductRating(productId: $productId) {
    averageScore
    totalReviews
    reviews {
      ratingValue
      reviewTitle
      reviewText
      reviewAuthor
      reviewDate
    }
  }
}
